# 全力模具制造 ERP 系统 — 实施说明文档

> **适用对象：** 实施工程师、系统运维人员  
> **版本：** v1.0 | 2026年8月

---

## 一、系统概述

本系统为鹤壁市全力模具制造有限公司的生产管理 ERP，覆盖**销售、采购、仓库、生产、质量、设备、模具、排班**八大业务模块，以**驾驶舱（数据看板）**为汇总展示入口。

**技术架构：** .NET 9 后端 API + Vue 3 前端，SQL Server 数据库，单体部署。

---

## 二、模块总览与菜单结构

```
驾驶舱（数据看板）
├── 销售管理 ─── 销售订单、发货管理
├── 采购管理 ─── 采购订单、到货管理
├── 仓库管理 ─── 库存查询、出入库操作、库存流水、库存预警
├── 排班管理 ─── 班次设置、排班计划
├── 生产管理 ─── 生产计划、冲压产量单、生产日报
├── 质量管理 ─── 质检记录、计量器具台账、量具申购、器具报废、检定处理
├── 设备管理 ─── 设备台账、维护记录
├── 模具管理 ─── 模具台账、模具制造计划
├── 基础数据 ─── 客户、供应商、原材料、产品、仓库、员工
└── 系统管理 ─── 用户管理（仅管理员）
```

---

## 三、核心数据流转关系

### 3.1 以"销售订单"为主线的业务全流程

这是系统最核心的数据流——从客户下单到产品交付的完整链路：

```
客户下单                排产              领料/生产           质检           发货
  │                     │                  │                │             │
  ▼                     ▼                  ▼                ▼             ▼
销售订单 ──────→ 生产计划 ──────→ 冲压产量单 ────→ 质检记录 ────→ 发货单
  │              │    │    │         │                │             │
  │              │    │    │         │                │             │
  │              │    │    └────→ 生产日报           │             │
  │              │    │             │                │             │
  ▼              ▼    ▼             ▼                ▼             ▼
订单明细      材料库存 产品库存   工序产量        质量结果       库存扣减
            (领用减少) (入库增加)               (合格/不合格)  (流水记录)
```

#### 第①步：销售订单（销售管理）

| 动作 | 数据变化 |
|------|----------|
| 创建销售订单 | 生成订单号 SO+时间戳，录入客户、产品、数量、单价 |
| 确认订单 | 状态从"草稿"→"确认" |
| 排产 | 状态→"已排产"，触发创建生产计划 |

**关键字段：** 订单号、客户（关联客户表）、交货日期、金额（自动汇总明细行）

#### 第②步：生产计划（生产管理）

生产计划是连接销售与生产的核心枢纽。

| 动作 | 数据变化 |
|------|----------|
| 创建生产计划 | 生成计划号 M+yyMM+序号（如 M26087），关联客户、产品、材料、计划数量 |
| 开始生产 | 状态→"进行中" |
| 完成生产 | 状态→"已完成"，自动记录实际完成时间 |

**生产计划通过"制号"（PlanNo）串联所有生产活动：**
- 冲压产量单 → 按制号记录每道工序的产量
- 生产日报 → 按制号记录每日汇总
- 质检记录 → 按制号记录质检结果
- 模具台账 → 通过模具编号关联产品

> **重要说明：** 制号（如 M01、M06）是字符串关联，不是数据库外键。多个业务表通过制号字符串匹配实现数据串联。

#### 第③步：冲压产量单（生产管理）

记录每台设备、每道工序的冲压产量，是生产数据的核心来源。

| 动作 | 数据变化 |
|------|----------|
| 创建产量单 | 自动生成单号 SC+时间戳，记录工序名称、设备编号、操作员、合格数、废品数、工时 |

**数据流向：**
- 产量数据汇总到驾驶舱 → 今日产量、月产量、工序分布
- 合格数累计 → 生产计划的"已完成数量"

#### 第④步：生产日报（生产管理）

按日汇总每个制号的生产情况，包含前日结转、领料、入库等信息。

**明细行（工序明细）：** 每道工序的设备号、合格数、废品数、工时。

#### 第⑤步：质检记录（质量管理）

| 动作 | 数据变化 |
|------|----------|
| 创建质检单 | 生成单号 QC+时间戳，记录制号、工序、检验数量、合格数、缺陷原因 |
| 判定结果 | 合格 / 不合格 / 返工 |

**数据流向：** 质检数据汇总到驾驶舱 → 合格率趋势、缺陷原因 TOP 排名

#### 第⑥步：发货单（销售管理）

| 动作 | 数据变化 |
|------|----------|
| 创建发货单 | 生成单号 DH+时间戳，选择仓库、录入发货产品和数量 |
| **自动联动** | ① 扣减产品库存 ② 记录库存流水（销售出库） ③ 更新销售订单已发货数量 ④ 自动更新订单状态（部分发货/已发货） |
| 删除发货单 | 回滚以上所有操作 |

---

### 3.2 以"采购订单"为主线的业务全流程

```
下采购单             到货入库           生产领用
    │                  │                 │
    ▼                  ▼                 ▼
采购订单 ────→ 采购到货单 ────→ 材料库存增加
   │              │                  │
   │              │                  │
   ▼              ▼                  ▼
订单明细      到货明细          库存流水记录
            (关联采购单)        (采购入库)
```

#### 采购订单

| 动作 | 数据变化 |
|------|----------|
| 创建采购订单 | 生成单号 PO+时间戳，选择供应商、录入材料和数量 |
| 下单 | 状态→"已下单" |

#### 采购到货单

| 动作 | 数据变化 |
|------|----------|
| 创建到货单 | 生成单号 SH+时间戳，选择仓库 |
| **自动联动** | ① 增加材料库存 ② 记录库存流水（采购入库） ③ 更新采购订单已收数量 ④ 自动更新订单状态（部分到货/已到货） |
| 删除到货单 | 回滚以上所有操作 |

---

### 3.3 仓库库存的核心流转

仓库是所有业务模块的数据交汇点，库存变动来自多个方向：

```
              ┌─────────────────────┐
              │      库 存 表        │
              │  (实时库存余额)      │
              └──────────┬──────────┘
                         │
     ┌───────────────────┼───────────────────┐
     │                   │                   │
     ▼                   ▼                   ▼
  ┌──────┐          ┌──────┐           ┌──────┐
  │ 入库 │          │ 出库 │           │ 调整 │
  └──┬───┘          └──┬───┘           └──┬───┘
     │                 │                   │
  ├─ 采购入库       ├─ 生产领用         ├─ 盘盈
  ├─ 车间入库       ├─ 销售出库         └─ 盘亏
  └─ 退件          └─ 发货退回
```

#### 六种触发库存变动的业务场景

| 场景 | 库存类型 | 变动方向 | 流水类型 | 触发时机 |
|------|----------|----------|----------|----------|
| 采购到货 | 材料 | 增加 | 采购入库 | 创建采购到货单 |
| 生产领用 | 材料 | 减少 | 生产领用 | 手动出入库操作 |
| 车间入库 | 产品 | 增加 | 车间入库 | 手动完工入库操作 |
| 销售发货 | 产品 | 减少 | 销售出库 | 创建发货单 |
| 发货退回 | 产品 | 增加 | 发货退回 | 删除发货单（回滚） |
| 到货退回 | 材料 | 减少 | 入库冲销 | 删除到货单（回滚） |
| 盘点调整 | 材料/产品 | 增/减 | 盘盈/盘亏 | 手动盘点调整 |

> **每次库存变动都自动生成库存流水记录**，记录操作前后的库存余额，确保可追溯。

#### 库存预警

系统自动监控：当库存 ≤ 安全库存时，在库存预警页面和驾驶舱中显示警告。

---

### 3.4 生产计划的"制号"串联机制

"制号"（PlanNo，如 M01、M06）是贯穿生产全流程的业务编号：

```
制号 M01（顶盖前横梁）
  │
  ├── 生产计划 ─── 计划数量 10,000 件，计划开始/结束时间
  ├── 冲压产量单 ── 按工序记录每次冲压的产量
  ├── 生产日报 ──── 每日汇总：领料、产量、入库
  ├── 质检记录 ──── 按工序检验合格/不合格
  └── 模具台账 ──── 该产品使用的模具信息
```

**实施要点：**
- 制号由系统自动生成（M + 年月 + 序号），不可手动修改
- 各模块通过制号字符串关联，非数据库强制约束
- 查询时通过制号筛选即可查看该项目的全部生产数据

---

### 3.5 模具制造计划的阶段管理

模具制造计划（用于新模具开发）有独立的阶段管理：

```
模具制造计划（如 MJ2026-001）
  │
  ├── 阶段1：编程2D ──── 计划开始/结束，实际开始/结束
  ├── 阶段2：编程3D ──── 状态：未开始/进行中/已完成/超期
  ├── 阶段3：备料
  ├── ...
  └── 阶段11：调试完成
```

计划列表自动显示进度百分比 = 已完成阶段数 / 总阶段数 × 100%

---

### 3.6 设备维护闭环

```
设备台账
  │
  ├── 记录设备基本信息、维护周期
  ├── 自动计算"是否超期维护"
  │
  ▼
维护记录
  │
  ├── 记录维护日期、类型（保养/维修/点检）、内容、费用
  ├── 创建维护记录 → 自动更新设备状态为"运行"、更新上次维护日期
  │
  ▼
设备状态循环：运行 → 维修 → 运行 → 停机 → 报废
```

---

### 3.7 计量器具全生命周期

```
量具申购（ToolApply）
  │ 审核通过 → 同意
  ▼
计量器具台账（MeasuringTool）
  │
  ├── 在用 → 按周期自动提醒检定
  ├── 检定处理（ToolCalibration）→ 合格/不合格
  ├── 停用 / 封存
  └── 器具报废（ToolScrap）
```

**检定周期管理：** 系统自动计算下次检定日期，超期未检定的器具在仪表盘预警。

---

## 四、数据关联全景图

### 4.1 实体关系总览

```
                        ┌──────────┐
                        │  客 户   │
                        └────┬─────┘
                  ┌──────────┼──────────┐
                  ▼          ▼          ▼
            ┌──────────┐ ┌──────────┐ ┌──────────┐
            │销售订单  │ │生产计划  │ │模具台账  │
            └────┬─────┘ └────┬─────┘ └────┬─────┘
                 │            │            │
            ┌────┴────┐  ┌────┴────┐  ┌────┴────┐
            │订单明细  │  │冲压产量单│  │模具计划  │
            └────┬────┘  │生产日报  │  └────┬────┘
                 │       │质检记录  │       │
            ┌────┴────┐  └─────────┘  ┌────┴────┐
            │ 发货单  │               │阶段明细  │
            └────┬────┘               └─────────┘
                 │
            ┌────┴────┐
            │发货明细  │
            └─────────┘

                        ┌──────────┐
                        │ 供 应 商 │
                        └────┬─────┘
                  ┌──────────┼──────────┐
                  ▼                     ▼
            ┌──────────┐          ┌──────────┐
            │采购订单  │          │模具计划  │(间接)
            └────┬─────┘          └──────────┘
            ┌────┴────┐
            │订单明细  │
            └────┬────┘
                 │
            ┌────┴────┐
            │采购到货单│
            └────┬────┘
            ┌────┴────┐
            │到货明细  │
            └─────────┘

                        ┌──────────┐
                        │  仓 库   │
                        └────┬─────┘
                  ┌──────────┼──────────┐
                  ▼          ▼          ▼
            ┌──────────┐ ┌──────────┐ ┌──────────┐
            │ 库 存   │ │库存流水   │ │发货/到货 │
            └──────────┘ └──────────┘ └──────────┘

                        ┌──────────┐
                        │  员 工   │
                        └────┬─────┘
                  ┌──────────┼──────────┐
                  ▼                     ▼
            ┌──────────┐          ┌──────────┐
            │排班计划  │          │产量单操作员│
            └──────────┘          └──────────┘

                        ┌──────────┐
                        │  设 备   │
                        └────┬─────┘
                  ┌──────────┼──────────┐
                  ▼                     ▼
            ┌──────────┐          ┌──────────┐
            │维护记录  │          │产量单设备  │
            └──────────┘          └──────────┘
```

### 4.2 引用关系明细表

| 业务实体 | 引用（外键） | 被引用方 | 说明 |
|----------|-------------|----------|------|
| 销售订单 | CustomerId | 客户 | 哪个客户下的单 |
| 订单明细 | SalesOrderId | 销售订单 | 属于哪个订单 |
| 订单明细 | ProductId | 产品 | 什么产品 |
| 发货单 | SalesOrderId | 销售订单 | 对应哪个订单 |
| 发货单 | CustomerId | 客户 | 发给谁 |
| 发货单 | WarehouseId | 仓库 | 从哪个仓库发货 |
| 发货明细 | DeliveryId | 发货单 | 属于哪批发货 |
| 发货明细 | ProductId | 产品 | 发什么产品 |
| 采购订单 | SupplierId | 供应商 | 向谁采购 |
| 订单明细 | MaterialId | 材料 | 采购什么材料 |
| 到货单 | PurchaseOrderId | 采购订单 | 对应哪个采购单 |
| 到货单 | WarehouseId | 仓库 | 入哪个仓库 |
| 到货明细 | MaterialId | 材料 | 到什么货 |
| 生产计划 | CustomerId | 客户 | 为哪个客户生产 |
| 生产计划 | ProductId | 产品 | 生产什么产品 |
| 生产计划 | MaterialId | 材料 | 用什么材料 |
| 产量单 | PlanNo（字符串） | 生产计划 | 通过制号关联（非外键） |
| 生产日报 | PlanNo（字符串） | 生产计划 | 通过制号关联 |
| 质检记录 | ProductId | 产品 | 检验什么产品 |
| 质检记录 | PlanNo（字符串） | 生产计划 | 通过制号关联 |
| 库存 | WarehouseId | 仓库 | 在哪个仓库 |
| 库存 | ItemType + ItemId | 材料/产品 | 多态关联：ItemType="材料"时ItemId=Material.Id，ItemType="产品"时ItemId=Product.Id |
| 模具 | CustomerId | 客户 | 为哪个客户 |
| 模具 | ProductId | 产品 | 生产什么产品 |
| 模具计划 | CustomerId | 客户 | 为哪个客户 |
| 模具计划阶段 | MoldPlanId | 模具计划 | 属于哪个计划 |
| 维护记录 | EquipmentId | 设备 | 维护哪台设备 |
| 排班计划 | EmployeeId | 员工 | 排谁的班 |
| 排班计划 | ShiftId | 班次 | 什么班次 |

---

## 五、状态流转规则

### 5.1 销售订单状态

```
草稿 → 确认 → 已排产 → 部分发货 → 已发货 → 完成
```

- **自动触发：** 创建发货单时自动更新（部分发货/已发货）；删除发货单时回滚为"确认"
- **保护规则：** "完成"或"已发货"状态不允许修改或删除

### 5.2 采购订单状态

```
草稿 → 已下单 → 部分到货 → 已到货 → 完成
```

- **自动触发：** 创建到货单时自动更新（部分到货/已到货）；删除到货单时回滚
- **保护规则：** "已到货"或"完成"状态不允许删除

### 5.3 生产计划状态

```
未开始 → 进行中 → 已完成
        ↘ 暂停
```

- **自动触发：** 设置"已完成"时自动记录实际完成时间

### 5.4 模具状态

```
设计 → 制造中 → 试模 → 调试完成 → 量产
                              ↘ 维修 → 量产
                                       ↘ 报废
```

### 5.5 设备状态

```
运行 → 维修 → 运行
     → 停机 → 运行
            → 报废
```

- **自动触发：** 创建维护记录时自动更新设备状态为"运行"、更新上次维护日期

### 5.6 计量器具状态

```
在用 → 待检 → 在用（检定合格）
          → 停用（检定不合格）
     → 封存
     → 报废
```

---

## 六、单号生成规则

| 业务 | 前缀 | 格式 | 示例 |
|------|------|------|------|
| 销售订单 | SO | SO + 年月日时分秒 | SO20260803154522 |
| 发货单 | DH | DH + 年月日时分秒 | DH20260806100000 |
| 采购订单 | PO | PO + 年月日时分秒 | PO20260805140000 |
| 采购到货 | SH | SH + 年月日时分秒 | SH20260805143000 |
| 生产计划 | M | M + 年月两位 + 序号 | M26087 |
| 冲压产量 | SC | SC + 年月日时分秒 | SC20260803090000 |
| 质检记录 | QC | QC + 年月日时分秒 | QC20260805080000 |
| 量具申购 | SQ | SQ + 年月日时分秒 | SQ20260805100000 |
| 器具报废 | BF | BF + 年月日时分秒 | BF20260805100000 |
| 检定处理 | JD | JD + 年月日时分秒 | JD20260805100000 |
| 模具计划 | MJ | MJ + 年月日时分秒 | MJ20260805100000 |

---

## 七、驾驶舱 KPI 指标

驾驶舱集中展示以下关键指标：

| 指标 | 计算来源 | 含义 |
|------|----------|------|
| 今日产量 | 今日产量单完成数合计 | 当日冲压产出 |
| 本月产量 | 本月产量单完成数合计 | 月度产出 |
| 本月销售额 | 本月销售订单金额合计 | 月度营收 |
| 本月采购额 | 本月采购订单金额合计 | 月度采购支出 |
| 库存预警数 | 库存 ≤ 安全库存的品种数 | 缺料风险 |
| 在产计划数 | 状态为"进行中"的生产计划数 | 当前生产负荷 |
| 设备运行率 | 运行设备数 / 总设备数 | 设备利用率 |
| 今日发货 | 今日发货单数 | 当日发货量 |
| 本月发货 | 本月发货单数 | 月度发货量 |
| 材料库存总量 | 所有材料库存数量之和 | 材料储备 |
| 产品库存总量 | 所有产品库存数量之和 | 成品储备 |

**图表数据：**
- 库存结构：按材料/产品分类的库存分布
- 生产进度：各制号的计划数量、完成数量、完成百分比
- 质量趋势：每日检验数、合格数、合格率折线图
- 缺陷排名：TOP 8 缺陷原因
- 销售/采购趋势：月度金额柱状图
- 产量趋势：月度产出/废品柱状图
- 工序分布：各工序的累计产量饼图
- 最近动态：系统最新 10 条业务操作

---

## 八、基础数据说明

### 8.1 客户/供应商

- 编号系统自动生成（C001、S001 格式）
- 供应商有"付款条件"字段（如月结30天）

### 8.2 原材料

- 分类：板材、模具钢、硬质合金等
- 单位：张、kg、把
- 有安全库存设置，低于此值触发预警

### 8.3 产品

- 关联客户零件号和模具编号
- **单冲出件数**（OneOutputs）：每冲一次出几个产品
- **工艺路线**：落料-拉延-修边-冲孔侧冲（固定四道工序）

### 8.4 仓库

- 默认两个仓库：原材料库、成品库
- 所有出入库操作必须选择仓库

### 8.5 员工

- 按部门管理，有在职状态
- 关联排班和产量单操作员

---

## 九、系统部署

### 9.1 运行环境要求

| 组件 | 要求 |
|------|------|
| 操作系统 | Windows Server 2016+ / Windows 10+ |
| .NET | .NET 9 Runtime + ASP.NET Core Hosting Bundle |
| 数据库 | SQL Server 2016+ |
| IIS | 需安装 ASP.NET Core Module V2 |
| 浏览器 | Chrome / Edge（推荐 Chrome） |

### 9.2 部署步骤

1. 安装 .NET 9 Hosting Bundle（含 ASP.NET Core Module）
2. 将发布目录（含 `wwwroot`、`web.config`、`QuanliERP.Api.dll`）复制到服务器
3. IIS 中创建站点，物理路径指向发布目录
4. 应用程序池设置为 **"无托管代码"**
5. 修改 `appsettings.json` 中的数据库连接字符串
6. 首次启动自动创建数据库并导入默认数据

### 9.3 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 全部功能 |
| 生产 | pro | 123456 | 生产管理模块 |
| 仓库 | wh | 123456 | 仓库管理模块 |
| 质量 | qa | 123456 | 质量管理模块 |
| 销售 | sale | 123456 | 销售管理模块 |

---

## 十、常见实施问题

### Q1：数据库连接失败

检查 `appsettings.json` 中 `ConnectionStrings.Default`：
- 本机开发用 `Server=.;Database=QuanliERP;Trusted_Connection=True;...`
- IIS 部署建议改用 SQL Server 账号：`Server=服务器IP;Database=QuanliERP;User Id=sa;Password=密码;...`

### Q2：IIS 500.30 错误

1. 确认已安装 ASP.NET Core Hosting Bundle
2. 应用程序池 → 基本设置 → .NET CLR 版本选 **"无代码"**
3. 确认 IIS_IUSRS 对发布目录有读写权限

### Q3：首次启动很慢

首次启动需执行 `EnsureCreated()` 创建数据库 + `Seed()` 导入默认数据，约 10-30 秒属正常。

### Q4：忘记管理员密码

删除数据库（或清空 Users 表），重启应用即可重新初始化默认账号。

---

## 十一、数据流速查表

| 操作 | 影响的表 | 自动联动 |
|------|----------|----------|
| 创建销售订单 | SalesOrder + SalesOrderItem | 无 |
| 创建发货单 | Delivery + DeliveryItem | 扣减产品库存、记录流水、更新订单状态 |
| 创建采购订单 | PurchaseOrder + PurchaseOrderItem | 无 |
| 创建到货单 | PurchaseReceipt + PurchaseReceiptItem | 增加材料库存、记录流水、更新采购单状态 |
| 创建冲压产量单 | ProductionOrder | 更新生产计划已完成数（汇总计算） |
| 创建质检记录 | QualityInspection | 无（数据汇总到驾驶舱） |
| 创建维护记录 | EquipmentMaintenance | 更新设备状态为"运行"、更新维护日期 |
| 手动出入库 | Inventory + InventoryLedger | 实时更新库存余额和流水 |
| 盘点调整 | Inventory + InventoryLedger | 生成盘盈/盘亏流水记录 |
